No se puede hacer el despliegue en heroku porque hay que registrar la tarjeta de crédito
# Leer a partir de Ejemplo para más claridad

#docker container ls
#docker rm -f <container-name>

# solucion a docker 
sudo chmod 666 /var/run/docker.sock

# abrir un puerto en el localhost
docker run -d -p 8080:80 --name

Ejemplo: 

docker build -t language-detection-app .

# y luego se utiliza el otro 

docker run -d -p 8080:80 language-detection-app

# Para ver si esta corriendo el container

docker container ls

# Para borrar el container 

docker rm -f <container-name>